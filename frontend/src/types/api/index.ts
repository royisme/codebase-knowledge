/**
 * Backend API types and converters
 *
 * This module provides TypeScript types that match the backend OpenAPI schema
 * and converter functions to transform between backend (snake_case) and frontend (camelCase) formats.
 */

export * from './auth'
export * from './converters'
